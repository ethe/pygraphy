
[tox]
envlist = flake8, py37, coverage

[testenv]
passenv = *
changedir =
    tests

commands =
    py.test

deps =
    pytest
    coverage
    pytest-cov
    pytest-asyncio

[testenv:flake8]
deps = flake8
commands = flake8 pygraphql --ignore E501,W503

[testenv:coverage]
commands =
    py.test --cov=pygraphql

[coverage:run]
branch = True
